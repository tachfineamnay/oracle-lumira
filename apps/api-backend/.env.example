#----------------------------------
# Configuration Générale
#----------------------------------
# Environnement d'exécution (development, production)
NODE_ENV=production
# Port sur lequel le serveur API écoute
PORT=3000

#----------------------------------
# Base de Données (MongoDB)
#----------------------------------
# URL de connexion complète à la base de données MongoDB
# Remplacez <username>, <password>, <cluster> et <database> par vos vraies valeurs
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>"

#----------------------------------
# Stockage Fichiers (Amazon S3)
#----------------------------------
# ID de la clé d'accès IAM avec les permissions S3
AWS_ACCESS_KEY_ID="your_aws_access_key_id"
# Clé d'accès secrète IAM
AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key"
# Région AWS où le bucket S3 a été créé (ex: eu-west-3)
AWS_REGION="your_aws_s3_region"
# Nom unique du bucket S3
AWS_S3_BUCKET_NAME="your_aws_s3_bucket_name"

#----------------------------------
# Paiements (Stripe)
#----------------------------------
# Clé secrète de l'API Stripe (toujours sk_live_... en production)
STRIPE_SECRET_KEY="sk_live_..."
# Secret pour la vérification de la signature des webhooks Stripe
STRIPE_WEBHOOK_SECRET="whsec_..."

#----------------------------------
# Sécurité (JWT)
#----------------------------------
# Chaîne de caractères longue et aléatoire pour signer les jetons JWT
JWT_SECRET="your_super_long_and_random_jwt_secret"

#----------------------------------
# Configuration CORS et Frontend
#----------------------------------
# Origines autorisées pour CORS (séparées par des virgules)
CORS_ORIGIN="https://oraclelumira.com,https://desk.oraclelumira.com"
# URL de base du frontend pour la génération de liens
FRONTEND_URL="https://oraclelumira.com"

#----------------------------------
# Configuration de Développement (optionnel)
#----------------------------------
# Autoriser la création directe de commandes sans Stripe (development only)
ALLOW_DIRECT_CLIENT_SUBMIT="false"
# Auto-création d'expert en développement (development only)
ENABLE_AUTO_CREATE_EXPERT="false"
# Mot de passe par défaut pour l'expert auto-créé (development only)
DEFAULT_EXPERT_PASSWORD="Lumira2025L"